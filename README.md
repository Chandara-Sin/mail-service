# MAIL-SERVICE

## For development

This is project use MakeFile

### create `config.yml`

```yaml
app:
  host: localhost
  port: "8080"
```

### Run server

serve on `http://localhost:8080`

```sh
make run
```
